import React from "react";
import "./card.css"

const Card = ({containerWidth, containerHeight}) => {
  return <div className="card-container" style={{width: containerWidth, height: containerHeight}}> </div>;
};


export default Card;
